export enum LogLevel {
    INFO = 'info',
    ERROR = 'error'
}

export enum EventModule {
    AUTH = 'auth',
    USERS = 'users',
    POSTS = 'posts',
    AGENTS = 'agents',
    EXTERNAL = 'external'
}
